# _local_launchers — 用户本机桌面启动器

这些 `.cmd` 仅在**用户本机**有效:它们 `explorer` 打开本地路径(`D:\`、`Z:\`、`Y:\`、`E:\`)
或启动本机已装软件(KiCad、嘉立创EDA、Keil、Proteus、Altium)。在云端/他机不可用,
故从仓库根目录迁来此处归档,保持根目录干净(去芜存精·迁移整理)。

> 仓库内真正跨环境可用的入口是根目录的 `lceda.cmd` / `lceda.ps1`(调用 `lceda_bridge/lceda_cli.py`)。
