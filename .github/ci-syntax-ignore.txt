# CI 语法门禁 · 豁免清单(正本清源)
#
# 下列文件在**最初提交时即被截断**(写入端按字节/大小截断),核心逻辑已随提交丢失,
# 无法在不臆造代码的前提下修复。它们自诞生起就无法编译,且被引用方已用 try/except
# 优雅降级(见 pcb_server.py 对 pcb_intent 的导入)。
#
# 策略:不臆造丢失的实现、也不删除用户文件,而是登记在此豁免清单——
#   * CI 对全仓其余 .py 强制 py_compile(挡住**新增**语法错误);
#   * 本清单内文件仅做**非阻断**报告,待日后补全后从清单移除。
#
# 每行一个相对路径(# 开头为注释)。
pcb_brain/pcb_intent.py
pcb_brain/pcb_self_loop.py
