"""
engine/ - Adaptive Workflow Engine

No fixed templates. No rigid pipelines. The engine adapts:
- Receives a specification (natural language or structured)
- Searches for relevant resources (local libs + network)
- Builds the board dynamically from real components
- Validates against design rules
- Iterates until correct

Like water flowing — finding the path of least resistance
while still reaching the destination.
"""
